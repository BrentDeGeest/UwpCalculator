<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style x:Name="BasicButton" x:Key="BasicButton" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Content" Value="A button"/>
        </Style>
        <Style x:Name="BasicButtonLeft" x:Key="BasicButtonLeft" TargetType="Button">
            <Setter Property="Margin" Value="100,10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Content" Value="A button"/>
        </Style>
        <Style x:Name="BasicButtonRight" x:Key="BasicButtonRight" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Content" Value="A button"/>
        </Style>
    </Page.Resources>
    
    <Grid Name="PlayArea">
        <Button Name="StartButton" HorizontalAlignment="Center" VerticalAlignment="Center" Click="StartButton_Click">A button</Button>
    </Grid>
</Page>
